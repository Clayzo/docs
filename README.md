# Clayzo documentation

Public docs for the Clayzo CLI, packages and agent skills, built with [Mintlify](https://mintlify.com).

## Development

```bash
npm i -g mint
mint dev
```

Preview at `http://localhost:3000`. `mint broken-links` checks internal links.

## Publishing

Changes to the default branch deploy automatically through the Mintlify GitHub app.
